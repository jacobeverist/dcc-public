{
  "_comment": "Default config for encoder-studio-embed.html, loaded by src/studio/embed.tsx via embedConfig.ts when no inline config (window global / ?config= / #hash) is provided. Edit this to change the embed's baseline layout without rebuilding. Schema: StudioEmbedConfig in src/studio/embedConfig.ts.",
  "encoders": [
    {
      "type": "interval-fixed",
      "n": 13,
      "w": 1,
      "lower_bound": 0.0,
      "upper_bound": 1.0
    }
  ],
  "viewBounds": [
    -1.0,
    2.0
  ],
  "scalarCursor": 0.5,
  "ui": {
    "cursor": true,
    "bits": true,
    "stacked": true,
    "demos": false,
    "controls": false,
    "plotrange": false,
    "picker": false,
    "refpoints": false,
    "stackedPanel": true,
    "subplots": true
  },
  "stacked": {
    "order": [
      "bins",
      "per-bin",
      "similarity",
      "bits-by-data",
      "projected-similarity",
      "decomposition",
      "similarity-heatmap",
      "features"
    ],
    "active": [
      "bins",
      "per-bin",
    ]
  },
  "interactive": false,
  "background": false
}
